#pragma once

#include <JuceHeader.h>

namespace Theme
{
    static const juce::Colour backgroundTop = juce::Colour::fromRGB(18, 18, 24);
    static const juce::Colour backgroundBottom = juce::Colour::fromRGB(30, 33, 45);
    static const juce::Colour panel = juce::Colour::fromRGB(35, 40, 58);
    static const juce::Colour panelLight = juce::Colour::fromRGB(45, 51, 70);
    static const juce::Colour panelBorder = juce::Colour::fromRGB(110, 130, 180);
    static const juce::Colour text = juce::Colour::fromRGB(232, 238, 245);
    static const juce::Colour accent = juce::Colour::fromRGB(74, 204, 255);
    static const juce::Colour accentSoft = juce::Colour::fromRGB(74, 204, 255).withAlpha(0.45f);
    static const juce::Colour disabled = juce::Colour::fromRGB(122, 136, 156);
}
